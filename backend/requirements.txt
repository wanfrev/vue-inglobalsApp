fastapi==0.115.6
uvicorn[standard]==0.34.0
gunicorn==23.0.0
python-dotenv==1.0.1
pydantic-settings==2.7.1
openai==1.59.3
# <2 a propósito: los wheels de NumPy 2.x están compilados asumiendo
# instrucciones x86-64-v2 (SSE4.2/POPCNT) que algunas CPUs virtuales de VPS
# no exponen — causa "RuntimeError: NumPy was built with baseline
# optimizations... but your machine doesn't support" al importar. Los
# wheels 1.26.x no tienen ese requisito. Verificado en el VPS de producción.
numpy<2
faiss-cpu==1.12.0
sentence-transformers==3.4.1
# Pineado a propósito: transformers >=4.50 reestructuró transformers.generation
# (GenerationMixin) de forma incompatible con sentence-transformers==3.4.1.
# 4.49.0 es la última versión antes de ese cambio.
transformers==4.49.0
pypdf2==3.0.1
python-docx==1.1.2
beautifulsoup4==4.12.3
langchain-text-splitters==0.3.5
python-multipart==0.0.20
